[package]
name = "hawk_backtester"
version = "0.3.0"
edition = "2021"

[dependencies]
polars = { version = "0.46.0", features = ["csv"] }
time = { version = "0.3", features = ["formatting", "parsing"] }
